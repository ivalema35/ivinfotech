# Copy to .env on the website host and restart the app.

# CRM bridge — required for career applications & contact forms to appear in CRM
CRM_BRIDGE_URL=https://crm.ivinfotech.ca
CRM_BRIDGE_TOKEN=change-me-long-random-secret

# CRM_BRIDGE_TOKEN must match the CRM backend CRM_BRIDGE_TOKEN exactly.
